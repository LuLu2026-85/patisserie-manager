# 设计交底 · 给 Claude Design

> 用法:第 ① 段复制进 Design 的「这是个什么 app?」输入框;第 ②③④ 段在 Design 问完问题、进入对话后整段粘贴过去。

---

## ① 这是个什么 app(复制进那个输入框)

一家法式甜品 / 面包店的自用管理软件,老板娘和店员每天在用。它管六件事:配方(每款甜品的配料、步骤、烤温烤时)、组件仓库(慕斯、饼底、淋面这类可复用的半成品,能被多个成品引用)、组合蛋糕(把组件一层层叠起来的成品)、材料百科(1800 多种进口原料和 469 个品牌的资料库)、进货与本店原料成本、商品与销售记录。

它是纯前端网页应用(React,数据全存在浏览器本地),同时装成 PWA 挂在手机主屏上。桌面端用来录数据和查资料,手机端在厨房现场看配方和步骤。

---

## ② 给谁用、在什么场景用

- **主要用户**:老板娘一个人,现在;未来加 1~2 个店员
- **桌面端(1280px 以上)**:录配方、整理材料百科、看成本、导数据。坐着用,信息密度可以高
- **手机端(390px 左右)**:**在厨房里、手上有面粉的时候看配方**。要能单手操作,字要够大,关键操作放在拇指够得着的地方
- **打印**:配方要能打印成纸稿贴在操作台上,这是一套独立的样式
- **双语**:界面和内容都是中日双语,顶部有个中文 / 日本語 切换按钮。**同一个标题中文可能 4 个字、日文可能 12 个字**,布局不能被长标题撑爆

---

## ③ 可以放开改的(别客气,全部推翻重来也行)

- **全套配色**。现在是暖米底 `#FCFAF6` + 咖啡 `#2D1B0E` + 焦糖 `#AC6B3A`,这套可以整个扔掉
- **全套字体**。现在标题用系统 serif(Georgia / 游明朝)做"法式优雅",这套也可以整个换
- **四张语义色表也可以整套重配**:五个「盆」的分组色、约 15 个风味族色(红果 / 柑橘 / 抹茶 / 巧克力……)、9 个知识标签色、8 个组件类别色
- **所有组件样式**:卡片、按钮、输入框、表格、标签、弹窗、顶部导航
- **间距 / 字号 / 圆角 / 阴影 / 层级**:现在完全没有体系,请从零建一套
- **kororā 字标**:见第 ⑤ 段

## ④ 不能坏掉的东西(功能约束,不是审美红线)

1. **五个「盆」必须还是五个一眼能分开的颜色**。这是配料分组,厨房里靠颜色认对应哪个搅拌盆,颜色可以全换,但五个之间的区分度不能丢。同理约 15 个风味族也要保证组内可区分
2. **10 个顶部 tab 的信息架构不要动**(商品 / 采购 / 配方一览 / 组件仓库 / 组合蛋糕 / 知识库 / 本店原料 / 供货商 / 材料百科 / 数据),只改样子
3. **中日双语切换按钮必须保留**,而且要在明显的位置
4. **配方打印稿要单独给一版打印规范**(黑白、A4、贴在操作台上能看清)
5. **手机端要真的能用**,不是把桌面版缩小

## ⑤ 顺便帮我做一版 kororā 字标(临时占位)

店名要改成 **kororā**,现在 app 顶部还是旧名 RURU。请顺手出一版字标先放进去,后面我会换成正式的自绘版,所以**不用死磕造字,做到"放在 app 顶部不掉价"就行**。

- **拼写**:`kororā`,全小写,末尾长音符 `ā` 是品牌记号,不能省
- **含义**:毛利语的「小蓝企鹅」,世界上最小的企鹅
- **副标(内容锁死,不要改字)**:`Boulangerie • Pâtisserie • Café`
- **品牌故事内核**:「最小的企鹅,走到温暖跟前」(出处是纪录片里最小的企鹅宝宝顶着风暴走向大部队挤进去取暖)
- **不要走**:奶油可爱系、卡通治愈系。企鹅本身已经够可爱了,字标要拉住,走高级、克制
- **可用素材**(见随附文件):企鹅透明底 PNG、单只企鹅线稿 SVG

## ⑥ 技术现状(影响你怎么给规范)

- **React 18 + Vite,全部是 inline 样式,项目里一个 CSS 文件都没有**
- 已经有一层设计常量对象 `T`(颜色 + 圆角 + 字体),被引用约 1470 次。**你给的色板和字体我可以一次性换进这个 `T` 里,全站同时生效**
- 但 `T` 里**没有间距 / 字号 / 阴影 / 层级**,这几样请务必给具体数值(比如 4px 网格、字号阶梯 12/13/14/16/20/24 这种),我要把它们补进 `T`
- 现在**没有 hover / focus / 过渡动效**,也没有响应式断点(inline 样式做不到)。我会为此加一个样式注入块,**请把 hover 态、focus 态、disabled 态、以及移动端断点一并写进规范**
- 顶部导航现在是 10 个 tab 横滚,手机上很挤,这块欢迎重新设计
- 请给的东西:**色板(含语义色表) / 字体栈与字号阶梯 / 间距系统 / 圆角与阴影 / 按钮和标签的各状态 / 卡片规范 / 表单规范 / 表格与列表规范 / 移动端断点与适配规则 / 打印规范**

---

## ⑦ Design 追问的九个问题 · 答案

**Q1 具体想改哪几个页面?**
> 配方一览(列表页)+ 配方详情页。这两页每天用得最多,配方详情也是全 app 最复杂的一页(分组配料表 + 三色标签 + 中日法三语 + 步骤列表 + 成本汇总),这一页的体系立住了,其余 8 个 tab 我照着套就行。有余力的话再加一版手机端的配方详情。

**Q2 现在最不满意的地方是什么?**
> 1. **宽屏上内容贴着屏幕左右边缘通栏铺开**,没有内容最大宽度,看起来很廉价
> 2. **层级不清**:标题、副标题、正文的字号差别太小,整页几乎一个灰度,一眼扫不出重点在哪
> 3. **间距完全没有节奏**:有的地方挤成一团,有的地方空一大块,px 和 rem 两套单位混着用
> 4. **半新半旧两套颜色并存**(暖米咖啡系 + 没换干净的旧冷灰),所以整体不统一、发脏
> 5. **完全没有交互反馈**:鼠标移上去毫无变化,按钮不像能点的
> 6. **配料表信息太密**:一行里塞了勾选框、标签、中文名、日文名、法文名、用量、单位、品牌、成本、备注,眼睛找不到落点
> 7. **卡片没有轻重之分**:全是白底加一条细边,重要的和不重要的长得一模一样
> 8. **手机端最糟**:顶部 10 个 tab 只露得出 5 个要横滑,配料表被挤成折行,在厨房手上有面粉的时候很难用

**Q3 有品牌规范要遵守吗?** → 选 `完全没有,你来定`
(店名和企鹅素材已经在上传文件里给了,但配色和字体完全放开)

**Q4 要几个方案对比吗?** → 选 `给我 2-3 个不同风格的方案对比`

**Q5 希望在哪些方面拉开差异?** → 勾 `视觉风格差异大` + `信息密度(紧凑 vs 宽松)`
(不勾布局结构和交互方式:三个维度都变的话方案之间没法直接比)

**Q6 要能点击操作的原型吗?** → 选 `关键交互能演示即可`
(我要的是 hover 态、focus 态、disabled 态长什么样,不需要能真的跳转)

**Q7 信息密度偏好(1~10)** → 拉到 **7**
(这是专业工具,厨房里要一眼看完整张配料表,不能为了好看拆到三屏;但现在大概是 9 那种密度,太挤了,要有呼吸感)

**Q8 有喜欢的参考 app 或网站吗?**
> 工具形态参考:**Linear**(层级清晰、克制的灰阶、hover 反馈舒服)、**Notion**(信息密度高但不乱)、**Things 3**(留白和字号的节奏感)。
> 气质参考:日本独立面包店和美术馆印刷品那种排版 —— 字距拉开的现代无衬线、大量留白、几乎没有装饰。具体可以看 **Comme'N TOKYO**、**SIMOSE 美术馆**。
> **不要**:奶油可爱系、圆润卡通、大面积粉色 / 马卡龙色那种"甜品店刻板印象"。

**Q9 文案怎么处理?** → 选 `用我现有的文案,别改`
(界面文案是中日双语成对的字段,改文案要连带动代码里几百处;日文是给日本店员看的,不能让 AI 重写)

---

## 附:随附文件清单

| 文件 | 是什么 |
|---|---|
| `app截图_给Design/` 里的 PC_*.png | 现有界面,1280px 桌面宽,11 张 |
| `app截图_给Design/` 里的 M_*.png | 现有界面,390px 手机宽,5 张 |
| `penguins_transparent.png` | 企鹅群,透明底 |
| `penguin_line.svg` | 单只企鹅线稿 |
| 本地代码文件夹 | `C:\Users\11508\Desktop\店铺数据\patisserie-manager` |
